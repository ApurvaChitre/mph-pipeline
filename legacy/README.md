# Legacy files

These files were in the original repo snapshot and are kept only for reference:

- `stack_pheno_create_cov.R` (hard-coded paths)
- `make_cohort_grms.R` (hard-coded / demo paths)
- `grm_job.sh` (older GRM job)

The maintained, parameterized versions live in:

- `scripts/` (R scripts)
- `slurm/` (sbatch scripts)
- `bin/submit_mph_pipeline.sh` (one-command submission)
